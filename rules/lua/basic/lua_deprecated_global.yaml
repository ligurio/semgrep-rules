rules:
  - id: lua_deprecated_global
    metadata:
      author: Sergey Bronnikov <estetus@gmail.com>
      description: Using deprecated globals.
    message: TODO
    languages: [lua]
    severity: WARNING
    pattern-either:
      - pattern: ypcall(...)

# Some global names exist for compatibility but their use is discouraged. This
# mostly affects globals introduced by Roblox, and since they can have
# problematic behavior or can break in the future, this warning highlights their
# uses:
